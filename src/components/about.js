import React from 'react';  //imporing for using our context (useContext)
// import NoteContext from '../context/notes/notescontext';  //importing context, else have to provide path in usecontext

const About = () => {
  return (
    <div>
      about Page
    </div>
  )
}

export default About;
